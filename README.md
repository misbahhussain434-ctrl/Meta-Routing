# Meta-Routing
Meta Routing is an AI powered system that automatically analyzes queries and directs to the AI model or processing pipeline. Instead of sending every request to a single model, the system evaluates the query type, complexity, domain, and requirements,  selects the optimal route to achieve better performance, lower cost, and higher accuracy. 
